services:
  redis:
    image: redis
    ports:
      - "6379:6379"
  matching_engine:
    image: matching_engine
    ports:
      - "3000:3000"

  websocket:
    image: websocket
    ports:
      - "8000:8000"

networks:
  # The presence of these objects is sufficient to define them
  front-tier: {}
  back-tier: {}